"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const hardhat_1 = __importDefault(require("hardhat"));
process.stdin.setEncoding('utf8');
let inputData = '';
process.stdin.on('data', (chunk) => {
    inputData += chunk;
});
process.stdin.on('end', async () => {
    const taskArgs = JSON.parse(inputData);
    await runHardhatSimulation(taskArgs);
});
const runHardhatSimulation = async (taskArgs) => {
    const { transactions, } = await hardhat_1.default.run('sphinxSimulateDeployment', taskArgs);
    process.stdout.write(JSON.stringify({ transactions }));
};
let getaddrinfoErrors = 0;
process.on('uncaughtException', (error) => {
    if (error.message.includes('getaddrinfo ENOTFOUND') &&
        getaddrinfoErrors < 25) {
        getaddrinfoErrors += 1;
        return;
    }
    else {
        process.stdout.write(JSON.stringify({ message: error.message, stack: error.stack }));
        process.exit(1);
    }
});
//# sourceMappingURL=hardhatRunner.js.map
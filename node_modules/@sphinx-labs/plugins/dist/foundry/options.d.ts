import { FoundryToml } from './types';
export declare const cleanPath: (dirtyPath: string) => string;
export declare const resolvePaths: (outPath: string, buildInfoPath: string) => {
    artifactFolder: string;
    buildInfoFolder: string;
    deploymentFolder: string;
};
export declare const checkRequiredTomlOptions: (toml: FoundryToml) => void;
export declare const getFoundryToml: () => Promise<FoundryToml>;
